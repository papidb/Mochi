const transactionTypes = {
  TRANSFER: 'transfer',
  WITHDRAWAL: 'withdrawal',
  // specify in the event of other payment provider
  FUNDING_PAYSTACK: 'funding_paystack',
};

module.exports = transactionTypes;
